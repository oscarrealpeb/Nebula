# Nebula

Proyecto principal en Flutter para Android.

## Estructura

- `flutter_app/`: codigo fuente de la app.
- `docs/`: notas de implementacion y decisiones.

## Ejecutar

1. Entra a `flutter_app/`.
2. Ejecuta `flutter pub get`.
3. Ejecuta `flutter run -d <device_id>`.
